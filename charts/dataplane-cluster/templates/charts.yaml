{{- if not .Values.cluster }}
{{- fail "values.cluster é obrigatório (ex.: spoke-01)" }}
{{- end }}
{{- range .Values.charts }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "dataplane-{{ $.Values.cluster }}-{{ .name }}"
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  project: default
  source:
    repoURL: http://gogs.gogs.svc.cluster.local:3000/gitadmin/dataplanes.git
    targetRevision: main
    path: "{{ .chart }}"
    helm:
      releaseName: "{{ $.Values.cluster }}-{{ .name }}"
      valuesObject:
        {{- toYaml (.values | default dict) | nindent 8 }}
  destination:
    name: "{{ $.Values.cluster }}"
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
{{- end }}
