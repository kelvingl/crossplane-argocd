apiVersion: v1
kind: Service
metadata:
  name: floci-ui
  namespace: floci
spec:
  selector:
    app: floci-ui
  ports:
    - name: web
      port: 4500
      targetPort: 4500
    - name: api
      port: 4501
      targetPort: 4501
