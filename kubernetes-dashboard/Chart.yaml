apiVersion: v2
name: kubernetes-dashboard
description: Kubernetes Dashboard, without authentication — deployed by the dataplane-dashboard ApplicationSet into one filtered spoke only
type: application
version: 0.1.0
appVersion: "v2.7.0"
