# spoke-03: dataplane cluster criada só para validar feature 003 User Story 1
# (ver dataplanes/spoke-03.yaml e specs/003-vcluster-dataplanes/quickstart.md).
apiVersion: kubernetes.crossplane.io/v1alpha1
kind: ProviderConfig
metadata:
  name: spoke-03
  labels:
    lab.example.org/dataplane: spoke-03
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: spoke-03
      name: vc-spoke-03
      key: config
